% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_synthetic_reef_landscape.R
\name{create_synthetic_reef_landscape}
\alias{create_synthetic_reef_landscape}
\title{Create Synthetic Reef Landscape with Benthos and Disturbances}
\usage{
create_synthetic_reef_landscape(
  spatial_grid,
  config,
  include_disturbances = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{spatial_grid}{An sf object representing the spatial grid over which to simulate reefs.}

\item{config}{A list of configuration parameters controlling the simulation, including:
- \code{seed}: random seed
- \code{model}, \code{psill}, \code{range}, \code{nugget}: variogram/SPDE parameters
- \code{patch_threshold}, \code{reef_width}: reef patch parameters
- \code{years}: numeric vector of years to simulate
- \code{dhw_weight}, \code{cyc_weight}, \code{other_weight}: weights for disturbance layers
- \code{hcc_cover_range}, \code{hcc_growth}: baseline range and growth rate of hard coral
- \code{sc_cover_range}, \code{sc_growth}: baseline range and growth rate of soft coral}

\item{include_disturbances}{Logical; if TRUE, reef-level disturbances (CYC, DHW, OTHER) are included in the output.}

\item{verbose}{Logical; if TRUE, prints progress messages during simulation.}
}
\value{
A data.frame containing reef-level benthos data (\code{HCC}, \code{SC}, \code{MA}) and, if \code{include_disturbances = TRUE},
disturbance values (\code{CYC}, \code{DHW}, \code{OTHER}), along with reef coordinates.
}
\description{
Create Synthetic Reef Landscape
}
\details{
Generates a synthetic reef landscape from a spatial grid and configuration parameters.
This includes generating synthetic fields, patches, and reefs, SPDE projections,
baseline and synthetic benthos layers (hard coral, soft coral, macroalgae),
and optionally reef-level disturbance layers (CYC, DHW, OTHER). Reef coordinates are
preserved in the output data frame.
}
\author{
Murray
}
