% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_design_large_scale_random.R
\name{sampling_design_large_scale_random}
\alias{sampling_design_large_scale_random}
\title{Large‐scale Randomised Sampling Design}
\usage{
sampling_design_large_scale_random(data_reefs_pts_sf, config_lrge)
}
\arguments{
\item{data_reefs_pts_sf}{An \code{sf} object representing the full field. Must contain:
\itemize{
\item \code{Year} — survey year (numeric)
\item \code{Reef} — unique reef identifier
\item \code{HCC}, \code{SC}, \code{MA} — benthic cover values (logit scale)
\item \code{geometry} — spatial geometry
}}

\item{config_lrge}{A list with:
\itemize{
\item \code{n_locs} — number of reef locations to select
\item \code{n_sites} — number of sites per selected reef
\item \code{seed} — random seed for reproducibility
}}
}
\value{
An \code{sf} object representing the large-scale randomised sampling design.
Includes selected reefs, sampled sites, valid selected years, and all
associated cover values (still on the logit scale).
}
\description{
Large‐scale Randomised Sampling Design
}
\details{
Randomly selects reef locations and a fixed number of sites within each
selected location. Valid survey years are then sampled per reef using
temporal constraints (via \code{sample_years_with_condition()}).

The function:
\itemize{
\item Randomly samples reef locations
\item Randomly selects a fixed number of sites within each reef
\item Uses \code{sample_years_with_condition()} to select years per reef
\item Filters the dataset to retain only valid reef–year combinations
}
}
\author{
Julie
}
