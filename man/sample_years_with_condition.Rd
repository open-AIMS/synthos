% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_years_with_condition.R
\name{sample_years_with_condition}
\alias{sample_years_with_condition}
\title{Constrained Year Subsampling}
\usage{
sample_years_with_condition(
  years,
  min_n = 2,
  max_n = 15,
  max_gap = 5,
  max_tries = 100
)
}
\arguments{
\item{years}{A numeric vector of years to sample from.}

\item{min_n}{Minimum number of years required in the sampled subset.}

\item{max_n}{Maximum number of years allowed in the sampled subset.}

\item{max_gap}{Maximum allowable gap (in years) between consecutive
selected years.}

\item{max_tries}{Maximum number of attempts before returning \code{NULL}
if no valid combination is found.}
}
\value{
A sorted numeric vector of sampled years satisfying the constraints,
or \code{NULL} if no valid combination is found after \code{max_tries} attempts.
}
\description{
Sample Years Under Temporal Constraints
}
\details{
Randomly selects a subset of years while enforcing constraints on minimum
sample size, maximum sample size, and the maximum allowable temporal gap
between consecutive selected years. Useful for generating realistic temporal
sampling schemes in synthetic or constrained ecological datasets.

The function:
\itemize{
\item Ensures sampled years fall within the specified range of sample sizes
\item Enforces a temporal continuity rule (\code{diff(years) <= max_gap})
\item Randomly tries up to \code{max_tries} combinations before giving up
}
}
\author{
Julie
}
