% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_synthos.R
\name{plot_synthos}
\alias{plot_synthos}
\title{Plot Benthic Cover Dynamics}
\usage{
plot_synthos(synthos_data, type = "")
}
\arguments{
\item{synthos_data}{A data frame containing benthic survey observations.
Required variables depend on \code{data_type}:
\itemize{
\item \code{survey_depth} – depth category
\item \code{project_name} – project identifier
\item \code{site_name} – site name (e.g., "Reef10 Site 1")
\item \code{survey_start_date} – survey timestamp
\item \code{point_machine_classification} – benthic class (HCC, SC, MA, etc.)
\emph{used when \code{data_type = "points"}}
\item \code{cover} – numeric benthic cover values
\emph{used when \code{data_type = "cover"}}
}}

\item{type}{A character string specifying the type of plot to generate:
\itemize{
\item \code{"trajectories"} – time‐series plots (uses \code{plot_group()})
\item \code{"heatmaps"} – benthic cover heatmaps (uses \code{plot_map()})
}}

\item{data_type}{A character string indicating the structure of input data:
\itemize{
\item \code{"points"} – raw point-based benthic classifications
\item \code{"cover"} – pre-aggregated benthic cover values
}}
}
\value{
A named list of \code{ggplot} objects, where each plot corresponds to a
unique combination of depth and benthic classification group. The resulting
list can be visualised directly or iterated over using \code{purrr::walk()}.
}
\description{
Generate Benthic Cover Trajectory or Heatmap Plots
}
\details{
Produces a set of plots showing temporal patterns in benthic cover across
survey depths and benthic classification groups. The function supports two
data formats: point-level benthic classifications (\code{"points"}) and
aggregated benthic cover values (\code{"cover"}).
Depending on the \code{type} argument, the output is either a series of
time‐series trajectory plots or benthic‐cover heatmaps.

Based on \code{data_type}, the function:
\itemize{
\item aggregates point classifications into benthic cover proportions; or
\item averages numeric benthic cover values
}

Afterwards, it:
\itemize{
\item extracts year, reef, and site identifiers
\item splits the dataset by survey depth and classification
\item passes each subset to either \code{plot_group()} or \code{plot_map()}
\item returns all plots as a list
}
}
\seealso{
\code{\link[=plot_group]{plot_group()}}, \code{\link[=plot_map]{plot_map()}}
}
\author{
Julie
}
