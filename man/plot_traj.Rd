% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_traj.R
\name{plot_traj}
\alias{plot_traj}
\title{Plot Benthic Cover Trajectories}
\usage{
plot_traj(synthos_data)
}
\arguments{
\item{synthos_data}{A data frame containing benthic survey observations. Must
include (depending on \code{data_type}):
\itemize{
\item \code{survey_depth} – depth category
\item \code{project_name} – project identifier
\item \code{site_name} – site identifier (e.g., "Reef10 Site 1")
\item \code{survey_start_date} – timestamp of survey
\item \code{point_machine_classification} – benthic class (HCC, SC, MA, etc.) for \code{data_type = "points"}
\item \code{cover} – percentage cover values for \code{data_type = "cover"}
}}

\item{data_type}{A string specifying the data source structure:
\itemize{
\item \code{"points"} – uses raw point-level machine classifications
\item \code{"cover"} – uses pre-aggregated percentage cover values
}}
}
\value{
A named list of \code{ggplot} objects, each representing a benthic cover
trajectory over time for a unique combination of depth and classification
group. Plots can be viewed individually or iterated over using
\code{purrr::walk()}.
}
\description{
Generate Trajectory Plots for Benthic Cover
}
\details{
Creates a list of trajectory plots showing temporal changes in benthic cover
across survey depths and benthic classifications. The function handles two
data types: point-based benthic classifications ("points") and aggregated
percentage cover ("cover"). It returns a nested list of \code{ggplot} objects,
where each plot corresponds to a unique combination of depth and
point-machine-classification.

Depending on \code{data_type}, this function:
\itemize{
\item aggregates point-based classifications into cover proportions, or
\item averages numeric cover values
}

It then:
\itemize{
\item extracts temporal and site identifiers (year, reef, site)
\item splits the dataset by depth and classification
\item applies \code{plot_group()} to each data subset
\item returns all plots as a list
}
}
\seealso{
\code{\link[=plot_group]{plot_group()}}
}
\author{
Julie
}
